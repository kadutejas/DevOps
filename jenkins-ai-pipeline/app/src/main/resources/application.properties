spring.application.name=ai-pipeline-app
server.port=8080

# Actuator: expose health and metrics endpoints
management.endpoints.web.exposure.include=health,metrics,info
management.endpoint.health.show-details=always
